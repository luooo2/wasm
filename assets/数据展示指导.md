现在我有了足够的数据来给出完整的建议。以下是两部分回答：

---

## 一、数据呈现建议（哪些数据，什么格式）

**核心原则**：数据混乱的根源是"过多的中间结果"——很多数据（如所有皮尔逊系数、所有回归R²）并不强，不宜全部塞入正文。建议分三层：**必须呈现、推荐呈现、附录备查**。

### 必须呈现的数据（5项）

| 序号 | 形式 | 内容 | 位置 |
|------|------|------|------|
| 表1 | 表格 | 标签分布 + ratio 描述性统计 | 4.2节 |
| 表2 | 表格 | MicroBench 按程序类别的标签分布 | 4.2节 |
| 图1 | 箱线图 | 四组 ratio 分布（Micro-JIT/AOT、Poly-JIT/AOT） | 4.2节 |
| 表3 | 表格 | PolyBench 全 kernel ratio 排名（附标签） | 4.2节 |
| 表4 | 表格 | 分类模型 LOO 结果对比 | 4.4节 |

### 推荐呈现的数据（2项）

| 序号 | 形式 | 内容 | 位置 |
|------|------|------|------|
| 图2 | 散点图 | PolyBench `ls_ratio` vs `ratio_jit_over_native`（体现显著相关性） | 4.2节 |
| 图3 | 水平条形图 | PolyBench RF 特征重要性 top-8 | 4.4节 |

### 不建议放入正文的数据

- 所有皮尔逊/斯皮尔曼系数的完整表格（之前已讨论，低 r 值没有说服力）
- 回归模型结果（LOO R² 全为负，仅可附带一句话说明）
- 三分类模型的完整混淆矩阵

---

## 二、可直接用于论文的文字与数据表

### 替换第 326 行占位符 `（结果.csv表格展示）` 的内容

以下是**表1**和**表2**的具体内容，以及衔接文字，可直接放入 4.2 节开头。

---

**表 4-1**　两类数据集的标签分布与 ratio 描述性统计（阈值 10%）

| 数据集 | 执行模式 | native-better | similar | wasm-better | 均值 | 中位数 | 标准差 | 最小值 | 最大值 |
|--------|----------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| MicroBenchmark（n=34） | Wasm JIT | 18（52.9%）| 10（29.4%）| 6（17.6%）| 1.44 | 1.15 | 1.09 | 0.04 | 6.29 |
| MicroBenchmark（n=34） | Wasm AOT | 18（52.9%）| 10（29.4%）| 6（17.6%）| 1.45 | 1.15 | 1.12 | 0.04 | 6.33 |
| PolyBench（n=30） | Wasm JIT | 12（40.0%）| 16（53.3%）| 2（6.7%）| 1.30 | 1.01 | 0.62 | 0.81 | 3.39 |
| PolyBench（n=30） | Wasm AOT | 12（40.0%）| 16（53.3%）| 2（6.7%）| 1.30 | 1.01 | 0.62 | 0.81 | 3.40 |

> 注：ratio 定义为 `median_wasm_ms / median_native_ms`，大于 1 表示 Wasm 慢于 native；JIT 与 AOT 标签完全一致。

---

**表 4-2**　MicroBenchmark 各类别程序标签分布（JIT 标签）

| 类别 | 数量 | native-better | similar | wasm-better |
|------|:---:|:---:|:---:|:---:|
| B1 计算密集（Compute-heavy） | 5 | 0 | 4 | 1 |
| B2 访存密集（Memory-heavy） | 7 | 4 | 2 | 1 |
| B3 分支密集（Branch-heavy） | 5 | 1 | 1 | 3 |
| B4 调用密集（Call-heavy） | 4 | 3 | 1 | 0 |
| B5 宿主交互密集（Host-interaction-heavy） | 7 | 6 | 0 | 1 |
| B6 内存分配密集（Allocation-heavy） | 6 | 4 | 2 | 0 |
| **合计** | **34** | **18** | **10** | **6** |

---

**表 4-3**　PolyBench 各 kernel 的 Wasm JIT/AOT ratio 及标签（按 JIT ratio 降序）

| 程序 | JIT ratio | AOT ratio | 标签 |
|------|:---------:|:---------:|:----:|
| `gemm` | 3.39 | 3.40 | native-better |
| `jacobi-1d` | 3.27 | 3.30 | native-better |
| `heat-3d` | 2.04 | 2.07 | native-better |
| `jacobi-2d` | 2.02 | 1.90 | native-better |
| `syrk` | 1.64 | 1.64 | native-better |
| `durbin` | 1.49 | 1.49 | native-better |
| `atax` | 1.40 | 1.33 | native-better |
| `fdtd-2d` | 1.34 | 1.34 | native-better |
| `floyd-warshall` | 1.26 | 1.26 | native-better |
| `2mm` | 1.22 | 1.24 | native-better |
| `doitgen` | 1.21 | 1.20 | native-better |
| `nussinov` | 1.16 | 1.18 | native-better |
| `trisolv` | 1.05 | 1.07 | similar |
| `adi` | 1.04 | 1.05 | similar |
| `gemver` | 1.04 | 0.96 | similar |
| `trmm` | 1.03 | 1.03 | similar |
| `3mm` | 1.01 | 1.01 | similar |
| `bicg` | 1.01 | 1.00 | similar |
| `seidel-2d` | 1.00 | 1.00 | similar |
| `cholesky` | 1.00 | 1.00 | similar |
| `covariance` | 1.00 | 0.98 | similar |
| `mvt` | 1.00 | 0.93 | similar |
| `symm` | 0.99 | 1.01 | similar |
| `correlation` | 0.99 | 0.98 | similar |
| `deriche` | 0.99 | 0.99 | similar |
| `gesummv` | 0.98 | 0.97 | similar |
| `ludcmp` | 0.96 | 0.96 | similar |
| `lu` | 0.96 | 0.97 | similar |
| `gramschmidt` | **0.89** | **0.86** | wasm-better |
| `syr2k` | **0.81** | **0.80** | wasm-better |

> 注：ratio 值取内部计时中位数之比，保留两位小数；标签依 10% 阈值划分。

---

### 4.4 节分类模型结果表（替换或补充现有内容）

**表 4-4**　二分类模型（native-better vs. nonnative-better）LOO 交叉验证结果

| 数据集 | 模型 | LOO 准确率 | 宏平均 F1 | 首要分裂特征 |
|--------|------|:----------:|:---------:|:----------:|
| MicroBenchmark | 决策树（depth=2） | **70.6%** | 0.706 | `compute_density` |
| MicroBenchmark | 随机森林 | 55.9% | 0.558 | `compute_density` |
| MicroBenchmark | 逻辑回归 | 44.1% | 0.441 | — |
| MicroBenchmark | SVM（线性核） | 47.1% | 0.469 | — |
| PolyBench | 逻辑回归 | **60.0%** | 0.593 | — |
| PolyBench | SVM（线性核） | **60.0%** | 0.593 | — |
| PolyBench | 随机森林 | 56.7% | 0.554 | `ls_ratio` |
| PolyBench | 决策树（depth=2） | 53.3% | 0.514 | `ls_ratio` |

---

### 图形建议（需要生成）

**图1 箱线图（最重要，必须有）**

绘制 4 个分组的 ratio 箱线图：`Micro-JIT`、`Micro-AOT`、`Poly-JIT`、`Poly-AOT`。x 轴为执行模式/数据集，y 轴为 ratio，对数刻度（`log`）或截断处理极端值（`host_time_loop` ratio=6.29，`host_getcwd_loop` ratio=0.04）。加水平虚线 `y=1.0`。

可运行以下代码生成：

```python
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

mb = pd.read_csv("data/results/labels_microbench_internal.csv")
# PolyBench需要从polybench_internal.csv汇总计算ratio

fig, ax = plt.subplots(figsize=(8, 5))
data = [mb['ratio_jit_over_native'], mb['ratio_aot_over_native'], ...]
ax.boxplot(data, labels=['Micro-JIT', 'Micro-AOT', 'Poly-JIT', 'Poly-AOT'])
ax.axhline(1.0, color='red', linestyle='--', alpha=0.7)
ax.set_ylabel('ratio_wasm_over_native')
ax.set_yscale('log')  # 或设置ylim截断
```

**图2 散点图（推荐）**

x 轴：`ls_ratio`，y 轴：`ratio_jit_over_native`，只用 PolyBench 的 30 个点，按标签着色（红=native-better，蓝=similar，绿=wasm-better），加趋势线，注明 Spearman ρ = −0.47（p = 0.009）。

**图3 特征重要性条形图（推荐）**

PolyBench 上 RF 回归任务的特征重要性 top-8：`ls_ratio`（32.6%）、`compute_mem_ratio`（15.0%）、`mem_instr_count`（8.7%）、`br_density`（8.4%）、`compute_density`（6.8%）、`avg_bb_size`（6.4%）、`total_instr_count`（6.2%）、`call_density`（4.6%）。水平条形图，配色与散点图一致，标注百分比。

---

### 关于现有 4.2 节文本的一处修改建议

第 326 行的 `（结果.csv表格展示）` 替换为以下衔接语，然后插入上述三张表和图：

> 表 4-1 汇总了两类数据集在 JIT 与 AOT 两种执行模式下的标签分布及 ratio 描述性统计。表 4-2 进一步给出 MicroBenchmark 按程序类别的标签分布，图 4-1 以箱线图形式展示四组 ratio 的整体分布情况。

其余已有段落（关于结构规律的分析文字）维持不变，直接接续在表格和图之后，效果是：**先用数据让读者建立量化印象，再用文字给出机制解读**，逻辑更清晰。

---

**总结一下数据取舍原则**：
- **放入正文**：有具体数值意义的分布表、典型程序 ratio 排名表、分类模型对比表；以及 ratio 分布箱线图和特征相关散点图（直观、有说服力）
- **只在文字中引用数字、不放完整表**：PolyBench 三个显著相关特征（值够小 p < 0.05 即可引用）、回归建模（仅说明 R² 为负、模型泛化受限）
- **直接删除**：所有 Microbench 特征相关系数表（p 值全不显著，不适合在论文正文中作为论据）