{
  "diagnosis": "High function_count, basic_block_count and compute_density for a recursive solver point at a large hot path with redundant locals.",
  "passes": ["--optimize-instructions", "--simplify-locals", "--coalesce-locals", "--merge-blocks", "--vacuum"],
  "expected_effect": "Peephole + local coalescing + block merging reduce per-call instruction count and CFG size, lowering instructions_retired and cycles."
}
