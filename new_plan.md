# Wasm 性能解释项目执行计划（保留原思路 + 严谨落地版）

## 0. 项目目标与边界

### 0.1 总目标
- 从“预测准确率优先”转向“机制解释优先”：
  - 识别哪些程序结构会放大/缩小 Wasm 与 Native 的性能差距；
  - 用静态特征 + 运行时事件（`perf`）构建可验证的解释链；
  - 给出 Wasm 接近 Native 的边界条件与反例说明。

### 0.2 非目标（避免失焦）
- 不追求 SOTA 预测精度；
- 不做大而全的全因果识别（采用“机制假设驱动的准因果验证”）；
- 不在早期阶段引入过多复杂模型（如 DNN）。

### 0.3 核心研究问题（保留原思路，收敛表达）
1. 哪类静态结构特征与性能差距显著相关？
2. 这些相关关系在运行时微架构事件上是否有一致证据？
3. 在何种输入规模/程序结构边界下，Wasm 可接近 Native？

---

## 1. 最小原型验证（MVP，先验证思路可行）

> 目标：在最小成本下验证“静态特征 + perf 事件 + 解释模型”这条路线是否可跑通、可产出一致结论。

### 1.1 MVP 范围
- 数据：从现有样本中先选 12–16 个程序（建议：MicroBenchmark 8 + PolyBench 4~8）。
- 模式：`native` / `wasm-jit` / `wasm-aot`。
- 输入规模：仅两档（small, large）。
- 静态特征：复用已有特征（`compute_density`, `ls_ratio`, `compute_mem_ratio`, `br_density`, `call_density` 等）。
- perf 指标（最小集）：
  - `cycles`
  - `instructions`
  - `branches`
  - `branch-misses`
  - `L1-dcache-loads`
  - `L1-dcache-load-misses`

### 1.2 MVP 假设（最多 3 条）
- H1：高 `compute_density` 在 large 输入下更可能对应更高 `ratio_wasm/native`。
- H2：高 `ls_ratio` 样本中，Wasm 与 Native 时间差距更易收敛。
- H3：当 `branch-miss` 差异上升时，Wasm 劣势更可能增大（控制流敏感样本）。

### 1.3 MVP 产物
- `data/results/mvp_perf_table.csv`：每个样本、每种模式、每种输入规模的时间与 perf 指标。
- `data/results/mvp_analysis.md`：3 条假设的验证结果（支持/不支持/不确定）。
- `data/results/mvp_figures/`：
  - 图1：输入规模分层的 `compute_density` vs `ratio`
  - 图2：`ls_ratio` 分桶下的 ratio 分布
  - 图3：`branch-misses` 差异 vs ratio

### 1.4 MVP 验收标准（Go / No-Go）
- Go 条件（满足任意 2 条即通过）：
  - 至少 10 个样本可稳定采集完整 perf 指标；
  - 至少 2 条假设方向与数据一致；
  - 图表可重复生成，重复实验方向一致（允许数值波动）。
- No-Go 条件：
  - perf 指标噪声过大无法复现实验方向；
  - 样本不足且无法补齐到 10+；
  - 关键脚本不可复用。

---

## 2. 完整执行计划（分阶段）

## 阶段 A：实验协议冻结（Week 1）

### 目标
- 固定实验环境与测量协议，确保后续数据可比较、可复现。

### 任务
- 定义统一编译参数（native/wasm-jit/wasm-aot）；
- 定义运行协议（预热、重复次数、统计口径：中位数 + IQR）；
- 固定系统设置（CPU 绑核、频率策略、后台干扰最小化）；
- 统一输出 schema（样本 ID、特征、时间、perf 事件、标签）。

### 产物
- `docs/experiment_protocol.md`
- `scripts/run_benchmark.ps1`（或 python orchestrator）
- `data/schema/measurement_schema.md`

### 验收
- 任意 3 个样本可一键复现实验并产出结构化结果。

---

## 阶段 B：MVP 实施与评审（Week 2）

### 目标
- 跑通最小原型，确认思路可行再扩展。

### 任务
- 完成 MVP 样本运行与 perf 采集；
- 生成 MVP 图表与假设检验结论；
- 召开一次“是否扩展”评审（Go / No-Go）。

### 产物
- `data/results/mvp_perf_table.csv`
- `data/results/mvp_analysis.md`
- `data/results/mvp_figures/*`

### 验收
- 满足 1.4 的 Go 条件。

---

## 阶段 C：数据集扩充（Week 3-4）

### 目标
- 提升代表性：从“微基准 + kernel”扩展到“真实程序切片”。

### 任务
- 构建三层数据：
  - L1 微基准（机制可控）
  - L2 PolyBench/kernel（中复杂度）
  - L3 真实程序切片（实用代表性）
- 建立纳入/剔除标准：
  - 可编译、可重复运行、输入可控、测量时长可接受；
- 建立结构覆盖矩阵（控制流/访存/调用/系统交互）。

### 真实程序建议来源（优先可落地）
- 压缩/解压、哈希/加密、文本处理、图像处理、小型数值计算工具；
- 优先 C/C++ 且能快速迁移到 WASI 工具链。

### 产物
- `data/datasets/dataset_manifest.csv`
- `data/datasets/coverage_matrix.md`
- `data/results/expanded_measurements.csv`

### 验收
- 总样本达到目标区间（建议 40–80）；
- 覆盖矩阵四个维度均有代表样本。

---

## 阶段 D：解释建模与机制验证（Week 5-6）

### 目标
- 形成“统计相关 + 解释模型 + 事件证据”三段式证据链。

### 任务
- 统计分析：
  - 分层相关（按输入规模、程序簇）；
  - 效应量与置信区间报告；
- 解释模型：
  - 线性模型（GLM/Lasso/Ridge）用于方向与可解释系数；
  - 树模型（RF/XGBoost）+ SHAP 用于非线性解释；
- 机制验证：
  - 选 3–5 个程序做结构干预（如减少分支复杂度/改变访存模式）观察增量变化。

### 子模块 D1：基于智能体的 IR 结构改造增量验证（建议并入 Week 6）

### 可行性结论
- 可行，且价值高：该步骤可把“相关性解释”升级为“可干预验证”，显著增强结论可信度。
- 建议定位为“准因果验证”，不直接宣称严格因果。

### 执行方式（最小可落地）
- 选择 3–5 个代表程序（每类机制至少 1 个）；
- 由智能体辅助生成 LLVM IR 改造版本（每个程序 1–2 个改造）；
- 保持功能等价与输入不变，仅改变目标结构特征；
- 重新跑 `native/jit/aot + perf`，比较改造前后增量收益。

### 优先改造类型（从低风险到高风险）
1. 控制流改造：降低分支复杂度、合并小基本块、简化不可预测分支路径；
2. 访存改造：提高局部性、减少随机访存模式、合并冗余 load/store；
3. 调用改造：减少高频小函数调用层级，评估边界跨越开销变化。

### 产物
- `data/results/ir_transforms/transform_manifest.csv`（记录每次改造的目标特征与改造类型）
- `data/results/ir_transforms/equivalence_check.md`（功能一致性检查）
- `data/results/ir_transforms/incremental_gain_report.md`（时间与 perf 增量结果）

### 验收标准
- 每个改造样本都通过功能一致性检查；
- 至少 60% 改造样本在目标方向上出现一致增量变化（如分支简化后 branch-miss 差异下降）；
- 至少 2 个 case 可形成“结构变化 -> 事件变化 -> 性能变化”的完整证据链。

### 产物
- `data/results/model_interpretation_report.md`
- `data/results/hypothesis_validation.md`
- `data/results/ablation_cases.md`
- 图表目录（相关图、SHAP 图、case 对比图）

### 验收
- 每条核心结论都有至少两类证据支持（统计 + 事件 / 模型 + case）。

---

## 阶段 E：边界条件总结与工程化封装（Week 7）

### 目标
- 输出可复用、可解释、可演进的项目成果。

### 任务
- 归纳边界条件：
  - Wasm 明显劣势场景；
  - Wasm 接近/可能优于 Native 场景；
- 固化 pipeline：
  - 数据采集、分析、作图一键化；
- 风险与局限性文档化。

### 产物
- `docs/final_findings.md`
- `docs/limitations_and_threats.md`
- `scripts/pipeline_run_all.ps1`

### 验收
- 新增 1 个程序样本可在 pipeline 中自动进入结果与图表。

---

## 3. 方法学与统计规范（保证“严谨”）

### 3.1 重复与稳健性
- 每个测量配置至少重复 N 次（建议 N>=15）；
- 时间指标用中位数，报告 IQR 与离群点规则；
- 对关键结论做 bootstrap 置信区间。

### 3.2 多重比较与共线性
- 多特征显著性报告需控制多重比较（如 FDR）；
- 对高共线特征（如 `ls_ratio` 与 `compute_mem_ratio`）做 VIF/相关阈值筛选，避免解释漂移。

### 3.3 结果表达约束
- 禁止仅凭单一模型重要性下结论；
- 明确区分“相关证据”“机制推断”“因果主张”三个层级；
- 所有图表必须附实验条件与样本数。

### 3.4 IR 改造语义一致性与纳入协议

#### 协议 P1：语义一致性协议（Equivalence Protocol）
- 每个 IR 改造版本必须通过统一 test harness 的差分测试；
- 差分项至少包含：返回码、标准输出/错误输出、输出文件内容（或摘要哈希）；
- 至少覆盖两档输入规模（small/large）和多组测试输入（建议 >=5 组）；
- 涉及浮点结果时采用容差比较（记录绝对误差与相对误差阈值）；
- 任一测试点不一致即判定该改造“语义未通过”，不得进入性能统计。

#### 协议 P2：双后端可编译协议（Dual-Backend Build Protocol）
- 每个 IR 改造版本必须能够同时产出 Native 与 Wasm 工件；
- 两条构建链均需通过：编译成功、链接成功、可执行成功（至少 1 组冒烟输入）；
- 若仅单后端可编译，判定为“不可比较样本”，仅记录失败原因，不纳入结论。

#### 协议 P3：样本纳入与剔除协议（Inclusion/Exclusion）
- 仅当同时满足以下条件，改造样本才可纳入增量分析：
  1. 通过 P1 语义一致性检查；
  2. 通过 P2 双后端可编译检查；
  3. 完成规定次数重复测量并产出完整时间与 perf 指标。
- 剔除样本必须在 `transform_manifest` 中显式标记原因（语义失败/构建失败/测量失败）。

#### 协议 P4：评估指标协议（Primary vs Supporting Metrics）
- 主指标固定为 Wasm vs Native 性能差距：
  - `ratio_jit_over_native`
  - `ratio_aot_over_native`
  - 改造前后 `delta_ratio`
- perf 事件为解释性辅助指标，不替代主指标；
- 结论需至少报告“主指标变化 + 对应 perf 方向变化”，形成结构化证据链。

---

## 4. 风险清单与应对

### 风险 1：perf 噪声大、重复性差
- 应对：固定 CPU、增加重复次数、优先方向一致性而非点估计精度。

### 风险 2：真实程序迁移 WASI 成本高
- 应对：先做“真实程序切片”而非整项目；建立替补样本池。

### 风险 3：样本扩充后分析复杂度爆炸
- 应对：先按程序簇分层分析，再做全局汇总；保持核心假设仅 3 条。

### 风险 4：模型结果互相矛盾
- 应对：优先信任协议稳定的统计与 case 证据，将模型作为辅助解释工具。

### 风险 5：IR 改造破坏语义或不可编译
- 应对：限定改造模板、先做小步变换、每次改造都做功能等价回归测试和编译可用性检查。

### 风险 6：改造收益不稳定（受输入规模或噪声影响）
- 应对：固定 small/large 双档输入并重复采样，报告方向一致性与置信区间，不只看单次绝对值。

### 静态特征边界与后续改进

当前特征主要由源码与 LLVM IR 静态提取得到，未显式纳入输入分布、运行时热点路径和数据访问轨迹等动态因素。该设定有利于低成本、可迁移的先验筛查，但会弱化对输入敏感行为（如分支可预测性、缓存局部性、有效循环次数）的解释能力。

为提升结论稳健性，后续将借鉴“静态 + 动态”融合方案：在保留静态特征主体的同时，引入轻量动态特征（如 perf 事件统计、路径/块执行频次摘要、输入规模分层统计），并在分层评估中比较纯静态与融合特征的解释一致性与增益。

---

## 5. 本周即可执行的任务清单（Next Actions）

1. 冻结 MVP 样本清单（12–16 个）并建立 `manifest`。
2. 写最小采集脚本：统一输出时间 + perf 指标到 CSV。
3. 跑 small/large 两档输入，生成 MVP 数据表。
4. 生成 3 张 MVP 图并完成 `mvp_analysis.md`。
5. 按 Go / No-Go 标准评审，决定是否进入扩充阶段。
6. 从 MVP 样本中选 1–2 个程序做 IR 小步改造试验，验证 D1 子模块可跑通。

---

## 6. 最终交付形态（面向完整项目）

- 一个可复现实验 pipeline（采集、分析、作图、报告）；
- 一个分层数据集（微基准 + kernel + 真实程序切片）；
- 一套可解释结论（结构特征 -> 事件证据 -> 边界条件）；
- 一份透明局限性说明与后续扩展路线。

> 这份计划保留了你“解释优先、引入 perf、扩展真实程序”的原始思路，并通过 MVP 先行机制降低试错成本，确保每一步都可执行、可验收、可复现。
